const [a, b, c , d] = [3, 5, 1, 15]

// Operadores aritméticos são operadores binários, porque utilizam dois operandos
const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

// utilizado um operador unário no resultado da divisão
console.log(soma, subtracao, multiplicacao, -divisao, modulo)

// Saída:

// 24 10 15 -5 1